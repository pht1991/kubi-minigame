# 2026-07-14 工作日志

## 阶段四：包体优化 + 构建问题排查
- 分析 `build/wechatgame` 体积：引擎占大头，Bullet(468K)+Spine(396K) wasm 为纯 2D 文字游戏无需
- 裁剪引擎模块：`settings/v2/packages/engine.json` includeModules 从 25 砍到 6 个（2d/base/gfx-webgl/intersection-2d/tween/ui）
- 开构建开关：`profiles/v2/packages/builder.json` experimentalEraseModules + md5Cache
- 升级 `fix-build-config.js` 为综合后处理脚本（修 libVersion + 包体报告 + 冗余检查），配套 `fix-build.bat`
- 修复 libVersion 被 Cocos 内置模板覆盖问题：根 `project.config.json` 不管用，必须事后脚本修
- 删除空 `assets/resources/` bundle（之前报 config.json not found）

## 排查 spine.wasm ENOENT
- 全量排查确认：当前构建产物完全干净，cc.js 零 wasm 引用、无 spine.wasm 文件
- 结论：报错来自微信开发者工具编译缓存（旧构建残留），非代码问题

## UI 优化：状态栏两行化 + 交互扁平化
### 状态栏 (StatusBar.ts)
- 格式从单行 `HP 100/100满腹 100/100...` 改为双行 `标题\n数值`
- 删掉全部进度条代码（setBar方法 + 5个barNode属性）
- 简化数值显示，去掉 /MAX_STATE 后缀只显示当前值

### 交互层级简化 (MainScene.ts)
- **背包**：砍掉 Tab 筛选中间层，直接显示物品列表；物品操作改 DialogPanel 弹窗（含详情信息+操作按钮），不再 push 操作网格页和详情页
- **地图**：去掉 Tab 筛选（全部/已探索/未探索），直接显示所有地点并带探索进度标注和 ✓ 标记
- **技能**：去掉 Tab 分类（全部/天赋/技能），直接列出所有技能并用【天】【技】标识区分类型
- **贸易**：去掉自定义数量购买弹窗，改为详情页内嵌快捷按钮（×1/×5/×10/×20/买满）
- **地牢**：楼层总览从嵌入网格格子的长列表改为「查看楼层总览」按钮 → DialogPanel 弹窗选择楼层

## 修复：Cocos 构建报 "Script missing or invalid" / "Missing class"
- 根因：改 MainScene.ts 时多处 Edit 破坏了方法边界，导致 TS 语法错误 → 脚本编译失败 → 场景里 GameManager 节点的 MainScene 组件解析不到 → 报 missing
- 三处语法错误（用 esbuild 解析器定位，比括号检查可靠）：
  1. 233行：`openBagFiltered` 重复方法头（一个空方法头 + 真实方法头重叠）
  2. 949行：`openMapFiltered` 方法头被删掉，只剩 body
  3. 1184行：贸易 `buy_max/buy` 分支 body 缺失，只剩空 else-if 头
- 修复后 MainScene.ts / StatusBar.ts 均通过 esbuild 校验
- scene `__type__` 引用：完整 uuid `6fc6ff41-...` 即标准格式（library 缓存里 MainScene 编译产物 `__type__` 也是完整 uuid），无需 mangled；只需确保 = meta uuid 即可
- **教训**：Cocos 多方法 Edit 时容易吃掉方法头/边界，改完务必用解析器校验语法
- **可复用调试法**：用 esbuild JS API (`transformSync(src,{loader:'ts'})`) 快速定位 Cocos TS 脚本语法错误行，比 `grep`/括号检查可靠

## UI 优化：网格统一 4 列 + 弹窗滚动遮挡修复
### 网格列数统一 (MainScene.ts)
- 之前各 GridPage 列数混乱（1/2/3/4 混用）：状态3、配方详情2、烹饪选食材3/结果2、建筑详情2、农田2、陷阱2、地图2、地点详情3、贸易详情2、技能3、转生1、统计2、帮助2、战斗3 等
- 全部 `columns: 1/2/3` → `columns: 4`（`replace_all` 批量改，共 32 处 GridPage）
- 注：GridComponent 的 cellWidth 固定 160，改列数不改变单元格宽度，只改变每行个数；4 列时 content 宽约 704px，在 750 设计宽内可放下。少单元格的页（如烹饪结果只有 1~2 格）在 4 列下会偏左，属可接受取舍

### 弹窗滚动遮挡修复 (DialogPanel.ts)
- 病因：ScrollView 顶部 y=380 落在标题区（标题 y=400，跨 370~430）内，重叠 50px；内容节点渲染层级在标题之上，滚动时选项会盖住/被标题区吃掉 → 顶部"小遮挡"
- 修复：
  - 标题上移到 y=415（跨 385~445），关闭按钮留在 y=420
  - ScrollView 顶部从 y=380 下移到 y=340（两轮微调）、高度 760→700（底到 -340，距面板底 -450 还有 110px）
  - 选项内容 topPadding 从 30→24→36、bottomPadding 24px，首行不再被标题区裁切遮挡
- **弹窗底部空白过多 → 面板高度自适应**：DialogPanel.ts 新增 `updateLayout(total)` 按内容行数算 panelH（titleReserve110/bottomReserve30/minScrollH200/minPanel300/maxPanel900），重绘 panel 边框、title/close/scroll 随面板顶边重定位，content 高度取 max(total, actualScrollH)；消除底部死边距空白
- esbuild 校验 MainScene.ts / StatusBar.ts / DialogPanel.ts 均 OK

## 备注：Cocos 报错 3817
- `Error 3817: component of 'GameManager' index 1 corrupted` = 节点上某组件损坏被引擎移除
- 用户确认根因是 Cocos 编辑器里 GameManager 节点的自定义脚本没重新挂上（之前改代码丢了引用），重新添加脚本即可，**无需代码改动**

## 玩法差异梳理（当前 vs 原版 KuBi）
- 产出 `outputs/玩法差异对比.md`：数据层 1:1 搬运（物品409/怪99/事件68/地点20/地牢16层/配方全套），核心系统框架全就位
- **5 处机制简化**：战斗(技能仅伤害加成)、地牢(状态机收敛)、贸易(易货→金币购买)、烹饪(无cooker箱)、装备(仅写currentEquip无栏/耐久)
- **3 项仅数据未实现**：阵营(campSaveData预留无UI)、酿酒(ALCO_DATA 4条无入口)、死亡界面(onPlayerDeath空桩/TODO)
- **内容禁用**：炼金5配方注释、1地点fountain注释
- 结论：约85%可见玩法框架+100%内容数据量已就位；补齐优先级 P0死亡界面+装备栏耐久 / P1地牢完整状态机+贸易易货+战斗技能独立 / P2阵营+酿酒+启用炼金

## P0 补齐：死亡界面 + 装备栏耐久闭环（已实现）
- **死亡界面**（原 TODO 空桩）：GameManager 加 `respawn()`（恢复 PLAYER_STATE_INIT 满状态）；MainScene 加 `_isDead` 守卫 + `onPlayerDeath`→`showDeathScreen`（DialogPanel 强制选择：转生/重生，蒙层×关闭时 if(_isDead) 重展，避免选完被重弹）；`getDeathCause()` 展示死因
- **装备致命 bug 修复**：ActionCombat 原读 `weapon?.attack`（数据无此字段）→ 武器攻击力恒为兜底 5，装备毫无收益。改为读 `weapon?.damage`（+`reiToDmg` 轮回加成），并新增 `calcPlayerAtk()`/`calcDamageReduce()` 真正接入武器伤害与防具 `dmgMul`/`reiToDef` 减伤
- **装备栏**：主页新增「装备」入口 → `openEquipPanel`（5 槽位 body/hand/foot/head/neck + 武器攻击汇总 + 耐久显示 + 卸下弹窗）；`buildEquipCells`/`onEquipCellClick`
- **ActionItem**：`getEquipSlot()` 推导槽位（equipType→weapon:hand→头身足颈）；`equip` 首次装备初始化 `durableSaveData`；新增 `unequip(slot)`
- **耐久闭环**：ActionCombat `decayWeapon()`（每次攻击−1，归零损坏卸下）/ `decayArmor()`（受击−1）；改 `attack`/`useSkill` 用实时 `calcPlayerAtk` 并接减伤+耐久
- **types.ts ItemData** 补字段：equipType/damage/weaponType/reiToDmg/reiToAtk/reiToDef/meleeMul/magicMul/shootMul/dmgMul（原版装备数据真实字段）
- esbuild 校验 7 文件全部 OK（MainScene/StatusBar/DialogPanel/ActionCombat/ActionItem/GameManager/types）
- **注意**：esbuild 只验语法不验类型；Cocos tsc 依赖编辑器最终编译。原版战斗公式（meleeMul 等是 +X% 还是 ×X 倍）为推测还原，平衡可能偏离原版

## P1 补齐：地牢前缀/陷阱 + 贸易易货 + 战斗技能独立效果（已实现）
- **全部 10 个文件 esbuild 语法校验 OK**（MainScene/StatusBar/DialogPanel/BattlePanel/ActionCombat/ActionItem/ActionDungeon/ActionTrade/GameManager/types）
- ### P1-3 战斗技能独立效果 + 冷却（ActionCombat.ts + BattlePanel.ts）
  - CombatState 加 `cds`(冷却表)/`guard`(防御姿态)/`dodge`(闪避姿态)/`prefixName`/`prefixPlayerDmgMul`/`prefixMagicResist`
  - `useSkill` 重写为分技能独立效果：melee 猛击×(1+0.6×lv) cd2 / magic 爆发×(1+0.6×lv)无视魔法抵抗 cd3 / shot 精准×95%命中 cd2 / def 防御姿态(本回合受击×0.5) cd3 / agile 闪避姿态(本回合怪命中25%) cd2
  - `SKILL_CD` 常量表；`tickCd()` 每回合末递减；`monsterCounter()` 统一怪反击（应用 guard/dodge/装备减伤）
  - 防御/敏捷等非武器技能**不磨损武器耐久**（仅 melee/magic/shot 调 decayWeapon）
  - BattlePanel `showSkillGrid` 改为显示 5 技能 + 等级 + 冷却（cd>0 禁用灰显）
- ### P1-1 地牢前缀怪物 + 陷阱（ActionDungeon.ts + MainScene + BattlePanel）
  - `rollPrefix()`：25% 概率取 PREFIX_DATA 前缀（排除 upper）；`probeExploreEnhanced` 战斗分支带 prefix 返回
  - 前缀修正（ActionCombat.init）：atk→怪伤害×(1+buff) / fat→怪HP×(1+buff) / def→玩家伤害×(1+buff)⁻¹ / magic→魔法抵抗 buff / agile→怪略强
  - 新增 `trap` 房间类型：roll<0.74 且非战斗 → 按层数缩放伤害 `round((8+stair*4)*(0.8~1.2))`，`playerStateChange({hp:-dmg})` 触发（playerStateChange 已自动 checkDeath → 陷阱致死走死亡界面）
  - 概率重排：商人(room%5或<0.12) / 战斗<0.60 / 陷阱<0.74 / 宝箱<0.92 / 空房
  - 接线：`triggerBattle(mstId, prefix?)` → `BattlePanel.startBattle(mstId, prefix?)` → `ActionCombat.init(mstId, prefix)`；`doDungeonExplore` 加 `case 'trap'`
  - 地牢页加危险提示 cell（info3）
- ### P1-2 贸易易货估值系统（ActionTrade.ts + MainScene）
  - `ActionTrade.barter(traderId, offerItem, offerQty)`：换得 = min(max, floor(offerQty×priceOf(offer)/priceOf(give)))；金币商人(give==='gold')不参与易货
  - 贸易详情页 `buildTradeDetailPage` 在非金币商人下加「易货」区：列出背包可估值物品（barter:<id> cell）
  - `showBarterDialog(traderId, offerItem)`：×1/×5/×10/换满 四档预览换得数（沿用 showDungeonMerchant 的"选择后原地刷新"模式）
- ### 文档同步
  - `outputs/玩法差异对比.md` 更新：P0/P1 标记已完成、第二节逐系统对照行更新、底部速览改为"≈92%框架+100%数据，剩余烹饪cooker/阵营/酿酒/炼金5配方"
- **提醒**：esbuild 仅验语法；技能独立效果数值为合理推测还原，真机手感可能需校准


## P2 内容完整度补齐（2026-07-14 下午，全部 esbuild 语法通过）
- **P2-3 启用 5 条炼金配方**：`data_studio.ts` 取消注释 magicPotion/strPotion/shootPotion/agiPotion/defPotion（含 building:magicTable + science:potion_2，其余4条无 building 限制）
- **P2-3 精华可饮用**：`ActionItem.use` 新增 upgrade+isDrink 处理——饮用永久提升 `skill[upgrade] += value`，解决了之前精华药剂变死物品的问题
- **P2-1 阵营冰火**：`GameManager.chooseCamp/get camp`（一次性）；`TimeSystem.applyDecay` 接入被动（火：满腹/水分×0.85、冰：精神×0.5）；`playerStateChange` 火之阵营体温下限-5；战斗火之阵营攻击×1.05；菜单加"选择阵营"入口+选择弹窗
- **P2-2 酿酒**：新增 `ActionBrew.ts`（brew/harvestBrew/getBrewSlots，时间型类比农田，产出 itemAmount×(1+alcoLevel*0.15)，上限3槽）；`GameManager.alcoSaveData` 字段+存档读写；建筑"酿酒桶"详情加酿造管理入口；制造网格移除误接的 alco 即时制造入口（改走建筑）
- **P2-4 烹饪 cooker 箱**：`boxSaveData['cooker']` 作炊具容器（resetToInit 初始化）；`ActionCook.cook` 改用炊具箱匹配/扣料，产出入背包；`MainScene` 烹饪入口改为炊具管理（添加食材/开始烹饪/清空/选两食材烹饪）
- 改动文件：GameManager.ts / types.ts(SaveData) / data_studio.ts / ActionItem.ts / ActionCombat.ts / TimeSystem.ts / ActionBrew.ts(新增) / ActionCook.ts(重写) / MainScene.ts / data/玩法差异对比.md
- 报告已更新：原版完整度 ~92%→~97%，剩余仅 1 处地点资源(fountain)禁用 + 季节深层玩法(P3)
- ⚠️ 阵营数值为合理推测还原（原版阵营机制数据未移植，campSaveData 原为占位）；最终仍需 Cocos tsc 编译确认类型

## P3 扩展：云存档（微信云开发 + 单云函数，已实现）
- 设计：**本地优先（local-first）**，云只做镜像 + 多设备恢复，避免冲突/断网卡死
  - `SaveManager.save()` 写本地后立即异步 push 到云端（fire-and-forget，失败仅告警不阻断）
  - 启动 `checkCloudOnLaunch()` 比对云端 updatedAt 与本地 savedAt：云端更新则弹"下载云端/保留本地"
  - 菜单新增「云存档」入口 → 面板显示状态 + 立即上传/下载云端按钮
- 新增文件：
  - `core/cloudConfig.ts`：CLOUD_CONFIG{env,enabled}，留空自动降级为纯本地（不动现有逻辑）
  - `core/CloudSaveProvider.ts`：封装 `wx.cloud.callFunction('kubiSave')`，可降级（env 不支持/未配 → 安全返回），上传合并并发、传 savedAt 让云端存同值 updatedAt（避免"刚存即误报冲突"）
  - `cloudfunctions/kubiSave/index.js` + `package.json`：云函数按 openid 存/取整份存档 JSON（`_openid` 自动隔离，管理员权限无需配集合安全规则）
- 改动：`types.ts` SaveData 加 `savedAt?`；`SaveManager.ts` 接 _syncToCloud/shouldOfferCloudRestore/downloadFromCloud/uploadToCloud/cloudStatusText；`MainScene.ts` 导入 + 启动检测 + 菜单云存档入口 + 面板
- 全部 17 文件 esbuild 语法校验 OK
- **待用户操作（部署侧）**：微信开发者工具开通云开发 → 部署 kubiSave 云函数 → 建集合 `kubi_saves` → 在 cloudConfig 填 env 并 enabled=true
- ⚠️ 冲突策略为 LWW（最后写入获胜）；用户"保留本地"后若本地早于云端，下次自动存档会覆盖云端较新进度——休闲游戏可接受，未做自动 merge

## P3 扩展：季节深层玩法（A+B 层，已实现）
- 原版核心机制（查玩家攻略确认）："冬天所有资源停产，只能交易/拾荒/狩猎/陷阱，连田、井水都停产" → **冬季停产 + 低温压力**
- 当前 season 只是 0~3 计数器（SEASON_CIRCLE=15 天/季），仅驱动时间文字 + 2 个冬季商人 + 1 地点；SEASON_CHANGE 事件 emit 但无监听
- 改动：
  - `TimeSystem.ts`：加 `isWinter()`(season===3)/`seasonName`；`applyDecay` 冬季追加体温流失 `WINTER_COLD_PER_HOUR=0.4/h`（不破坏现有 temp 钳制：非 temp 限 0~MAX，火阵营 temp 下限 -5）
  - `ActionBuilding.ts`：`plantCrop`/`harvestCrop` 冬季拦截（土地封冻）；新增 `collectWell()`（每日一次，base 8 + wellUpdate 等级×2，冬季结冰拦截）——接上原本沉睡的井取水（原仅有建筑数据+升级链+wellBonus 物品，无交互入口）
  - `MainScene.ts`：建筑详情加「取水」入口（冬季置灰）；`start()` 注册 `SEASON_CHANGE` 监听 → `onSeasonChange` 弹换季公告（冬预警停产/春恢复）
- 保持可用（冬季求生手段）：拾荒/狩猎/陷阱/交易（冬季商人已按 season 门控）
- 全部 17 文件 esbuild 语法校验 OK；原版完整度 ~97% → 季节深层玩法补齐
- ⚠️ 井产量/低温速率/每日一次为合理推测还原；wellBonus 物品（wellDig_1~7）尚未接入 collectWell 产量（目前用 wellUpdate 等级缩放），如需更忠实可后续接
- ⚠️ 仍待 Cocos tsc 编译最终确认类型

## 英文文案转中文（用户反馈"游戏中还有不少英文文案"）
- 全量扫描结论：游戏显示文案其实已 ~97% 中文（怪物/地点/物品/技能/配方 name 全中文；所有产出/需求显示走 `ITEM_DATA[id]?.name || id` 查表）。真正的用户可见英文仅 5 处：
  1. `StatusBar.ts:61` 状态栏 `HP` → `生命`
  2. `BattlePanel.ts:383` 怪物血条 `HP ` → `生命 `
  3. `BattlePanel.ts:387` 玩家血条 `HP ` → `生命 `
  4. `ActionCook.ts:70` 烹饪成功消息用原始配方 id（`烹饪了 ${recipe.name}`）→ 改为 `ITEM_DATA[recipe.name]?.name`（中文名）；需补 import ITEM_DATA
  5. `data.ts` COOK_DATA 两处 `flour`（果酱面包/种子面包需求）非可获取物品 → 显示英文"flour"且配方永远做不出 → 改为 `wheat`（小麦，可获取，显示中文且可制作）
- **排查方法**：用 Python 脚本扫全部 .ts 字符串字面量，再用"显示汇点"（`label.string`/`makeLabel`/`DialogPanel.show`/`name:`/`title:`）二次过滤，并 diff 所有被引用的物品 id 与 ITEM_DATA 键，定位 `flour` 缺失
- 4 文件改动，17 文件 esbuild 语法校验全 OK
- ⚠️ esbuild 仅验语法；"HP"→"生命"属显示文案改动，不影响逻辑。原版若真有面粉研磨链（无磨坊建筑）本作未移植，`wheat` 为合理替代

## 弹窗内英文术语排查（用户反馈"弹窗内还有英文术语"）
- 重做扫描：先扫全部 .ts 字符串字面量，再用"显示汇点"（`label.string`/`makeLabel`/`DialogPanel.show`/`name:`/`title:`/`desc:`/`type:` 等字段）二次过滤；发现动态拼出的英文不在静态字符串里，需看渲染点
- 真正在弹窗内显示的英文（已修）：
  1. `MainScene.ts:367` 物品详情弹窗 `类型: ${itemData.type}` → 物品 `type` 是内部英文分类（tool/weapon/food/cooked/equip/head/body/foot/poizon/各类 Bonus 等）→ 新增 `ITEM_TYPE_LABEL` 映射表，显示中文（工具/武器/食物/熟食/装备/头部装备…/未知加成…）；未知类型回退原值
  2. `MainScene.ts:375` 物品详情 `效果·${k}:` → 状态键（full/moist/san/temp/hp…）→ 新增 `STATE_LABEL` 映射表（饱腹/水分/精神/体温/生命…），未知回退原 key
  3. `BattlePanel.ts:309-310` 战斗技能弹窗 `${s.name} Lv${lv}` → "Lv" 英文 → 改为 `等级`（"格斗猛击 等级3 (冷却5)"）
- 已验证无英文的弹窗/面板：物品 reqStr 需求文本（diff 全部被引用的 require/get id 与 ITEM_DATA 键，0 缺失 → 不会回退英文 id）、事件/地点/技能/配方 desc、状态栏(已中文)、地图/统计/帮助面板(全中文)、战斗技能名(中文)
- 改动 2 文件（MainScene.ts、BattlePanel.ts），17 文件 esbuild 语法校验全 OK
- ⚠️ 仍待 Cocos tsc 编译最终确认类型；ITEM_TYPE_LABEL 已覆盖 data_item.ts 现有全部 type 取值

## 英文文案转中文（第三轮 — 用户截图"弹窗内还有英文术语"：制造面板配方名全英文）
- 用户截图制造面板（`openRecipeGrid`）：knife / flyKnife / stick / axe / blockAxe / hoe / pickaxe / shovel / poizondKnife / shortSword … 全部是英文 id
- **根因**：MAKE_DATA/ALCHEMY_DATA/MAGIC_DATA/SCIENCE_DATA/ALCO_DATA 全部 5 张制造表都 **没有 `name` 字段**，`recipe.name || key` 永远回退英文 key
- **修复** `MainScene.ts` 3 处：
  1. `:624` 配方列表 label：`recipe.name || key` → `ITEM_DATA[key]?.name || key`
  2. `:675` 配方详情标题 title：同上
  3. `:676` 面包屑 breadcrumb：同上
- 酿酒面板已确认无此问题（用 `recipe.desc` + `ITEM_DATA[recipe.itemGet]?.name`，已是中文）
- 所有 5 张制造表走同一个 `openRecipeGrid()` 方法 → 一次修复全覆盖
- 改动 1 文件（MainScene.ts），17 文件 esbuild 语法校验全 OK
- ⚠️ 仍待 Cocos tsc 编译最终确认类型
