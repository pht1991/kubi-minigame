# 2026-07-27 工作日志

## 陷阱管理页布局优化（ccf71fa）
- TrapPage.buildTrapPage 原用 columns:4 方形网格，可放置陷阱 name 把 desc/诱饵/产出/科技挤一行，4 列小格截断。
- 修复：columns:4→1；所有 cell 加 type:'list'+noTruncate（触发 GridComponent list 模式：单列满宽660/行高120/文本左顶/不截断）；可放置陷阱 name 用 \n 拆两行 `[放置] 诱饵标签` / `诱饵[需求] → 产出[物品]`，两行均在 100px 文本区(<3.8行)不裁切。
- 关键机制（GridComponent 253-257 + GridCell 67/118）：isListMode 由 cells.some(c=>c.type==='list') 触发，触发后整页单列；混合模式不行（非 list cell 走居中 else 分支），故所有 cell 须统一加 type:'list'。
- ts.transpileModule 41 文件 0 错误。

## 全量排查方网格 + 收尾布局修复（fef86d5）
- 排查：除主页(BuildPage:62 home)、状态页/战斗列表(MainScene)、大箱子物品格、制造/事件/技能列表、建筑/地图网格、菜单/设置/云存档/统计(短按钮或短值)外，仍 5 处「长文字挤 4 列小格被截断」问题页：酿酒管理/技能详情/转生页/帮助页/装备页。
- 背包额外 bug：BagPanel 格子耐久条(barY=-46 横跨整格)与数量×N(y=-42 右下)同 y 且 x 重叠→叠字。修复：hasDur 时数量上移 y=-14、耐久条下移 -48、名称压缩高 44；无耐久数量仍贴底。
- 5 处方网格统一改单列 list：BrewPage/SkillPage/MenuPage(转生+帮助)/BagPage(装备页 buildEquipCells+两处 push) 均 columns→1 且 cell 加 type:'list'+noTruncate。
- 仍保留 columns:4 的合理方网格（图标/菜单/短值）：主页、状态页、战斗列表、大箱子物品、制造主页、事件/技能列表、建筑/地图网格、菜单/设置/云存档/统计、农田管理。
- ts.transpileModule 41 文件 0 错误。

## 记忆同步（本次操作）
- 将旧会话 `C:\Users\shendushuke\WorkBuddy\2026-07-07-10-38-34\.workbuddy\memory\` 下的完整记忆（16 天日志 + MEMORY.md）整同步至项目 `.workbuddy\memory\`，覆盖旧版并补齐缺失日志。后续记忆读写统一在项目下进行，旧会话存档不再更新。

## 阶段四·适配优化改造（2026-07-27）
- 探查结论：UI 100% 代码绘制(assets 0 用户图片)，包体大头=被禁用却仍打进包的引擎默认天空盒 TextureCube(~874K)+1.5M 引擎；多分辨率显示适配已做好(FIXED_WIDTH+safeArea+fitContentArea)，缺性能分级。
- T1 去天空盒死重：MainScene.scene 的 cc.SkyboxInfo 三张 envmap 引用(d032ac98/6f01cf7f)置 null + 占位 Sprite(afc47931)置 null；scene JSON 校验合法、3 uuid 0 残留，预期砍 ~850K+。
- T3 引擎拆子包：profiles/v2/packages/wechatgame.json 两处 separateEngine:false→true，1.5M 引擎移出主包(主包 3.0M→~0.5M)；依赖 Cocos 构建自动写 subpackages，需真机验证。
- T2 性能分级：新建 assets/scripts/core/PerfTier.ts(无循环依赖,low/mid/high 三档,微信 getDeviceInfo().benchmarkLevel / iOS 内存 / H5 核心数); MainScene.onLoad 调 initPerfTier() 且低端机 game.frameRate=30; 暴露 getPerfTier/isLowPerf 供布局降级。
- 校验：ts.transpileModule 全量 72 文件 0 错误。
- 待用户侧验证：Cocos 构建+微信 devtools 看主包体积与 native 不再含 d0/6f png；真机看 [PerfTier] 日志与帧率。

## 提交与推送（2026-07-27 15:28）
- 先丢弃 Cocos 自动写入的 3 个无关配置改动：profiles/v2/packages/{builder,scene,utils}.json（仅时间戳/log 级别变化），保持工作树干净。
- commit `1f8edc8`（22 文件：阶段四三处改造 + 完整记忆同步）。
- push 成功：`git push 'https://pht-gitee:Wit123456!!@gitee.com/pht-gitee/kubi-minigame.git' master` → `fef86d5..1f8edc8 master -> master`。**验证**：内嵌凭据 URL 可绕过 Windows credential.helper 崩溃坑（origin remote 是普通 https 无凭据，直接 `git push origin` 会触发崩溃）。

## 远程仓库由 Gitee 切换到 GitHub SSH（2026-07-27 15:42）
- 决策：完全切到 GitHub（移除 gitee origin），且改用 SSH 而非 PAT/HTTPS。
- 本地 `~/.ssh/` 已有 `id_ed25519`/`id_rsa`，`ssh -T git@github.com` 返回 `Hi pht1991!` 认证通过（说明有 key 已注册到 pht1991 账号）。
- `git remote set-url origin git@github.com:pht1991/kubi-minigame.git`，gitee 不再作为 origin。
- `git push -u origin master` 成功：`* [new branch] master -> master`，并设 upstream tracking（以后 `git push` 即可，SSH 不走 credential.helper 不崩）。
- 注：原 Gitee 远程仓库 `pht-gitee/kubi-minigame` 仍保留，仅本地 origin 切走；后续主推 GitHub SSH，不再需要 PAT/内嵌凭据 URL。

## 浏览器版 GitHub Pages 发布准备（2026-07-27 15:47）
- 探查：当前机器全盘无 CocosCreator.exe，无法本地生成 web-mobile 产物（Cocos 3.x 构建必须靠编辑器二进制，无 CLI）。采用「用户本地构建 + 推 gh-pages」方案。
- 兼容性确认：wx 调用已全平台隔离（SaveManager fallback localStorage、PerfTier/CloudSaveProvider/MainScene._fetchSafeArea 均有 typeof wx 判断），浏览器版无需改代码即可运行。项目无美术图片，web 产物仅数 MB。
- 交付：deploy-web.bat(Windows) + deploy-web.sh(Mac/Linux) 一键构建 web-mobile 并推 gh-pages；docs/WEB_DEPLOY.md 发布说明。已 commit `64094b0` 推 GitHub。
- 后续：用户在装有 Cocos 的机器改 COCOS 路径后跑脚本 → 去 GitHub Settings→Pages 启用 gh-pages 分支即可访问 https://pht1991.github.io/kubi-minigame/ 。
