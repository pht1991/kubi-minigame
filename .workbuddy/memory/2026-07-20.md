# 2026-07-20 工作日志

## v18: 删除 TradeQtyPanel，交易数量统一复用 QuantityPanel
- **问题**：TradeQtyPanel（带滑块）按钮错位 + 滑块与 ±1 功能重复
- **方案**：删除 TradeQtyPanel.ts；扩展 QuantityPanel.show 加可选 `QtyOptions`（infoLines/confirmLabel/getPreview）；TradePanel 改为调用 QuantityPanel
- **QuantityPanel 扩展接口**：`show(title, max, onConfirm, { infoLines?, confirmLabel?, getPreview? })` — 向后兼容，不传 options 行为不变
- **布局自适应**：有 infoLines 时 Y 坐标自动下移；有 getPreview 时确认按钮位置跟随预览行数
- QSlider 现无引用者（仅剩自身定义），暂留文件
- commit `8ccd31b`，净减 138 行，57 文件 0 错误

## v18-hotfix: QuantityPanel.render 崩溃 (lines is not defined)
- **根因**：`previewLines` 在 `if(o?.getPreview){}` 块内声明为 `lines` const，却在块外确认按钮处用 `lines!.length` 引用 → 运行期 ReferenceError（transpileModule 未捕获，属块级作用域逻辑错误）
- **修复**：把预览数组提到 render 外层 `const previewLines = ...`，确认按钮用 `previewLines.length` 定位；`setQty` 改为直接 `this.render()` 整面板重建（去掉失效的 `_previewNode` 刷新分支）
- commit `21941c0`，0 错误

## v18-fix2: QuantityPanel 确定按钮被裁切
- **根因**：`panelH = 470` 固定值，交易场景有 infoLines(1行) + preview(2行) 时内容总高 ~428px 超出面板可视区，确定按钮被底边 Mask 裁切
- **修复**：`show()` 中动态算高（基础 420 + infoLines×26 + preview×26 + 60 余量），调用基类已有的 `resizePanel(h)` 同步 `_panel`/`_panelBg` UITransform 尺寸+重绘背景+重定位标题/关闭按钮。注意不能只改 `this.panelH` 属性——ModalPanel.show() 不重新 setContentSize，必须走 `resizePanel()` 才真正生效
- commit `3ec87ea`
