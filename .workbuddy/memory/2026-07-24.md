# 2026-07-24 工作日志

## 运行时/布局修复序列（真机验证）
- **状态栏文字被裁切（0784bb6）**：`_mkStatusLabel` UILabel 高度=文本精确高度 + 默认 overflow=CLAMP → 上下裁切。两行属性标签 height 48→52、时间标签 48→56 加余量。
- **还是不全（3b24674）**：根因 `UILabel.estimateHeight()` 不识别 `\n`：`"生命\n100".length=6` 被 charsPerLine=10 算成 1 行→setText 把 52px 缩回 30px→CLAMP 裁切。修复：先 split('\n') 逐段估算再求和，末尾+4px 余量。已写入 MEMORY.md 铁律。
- **真机滚动区中间被遮挡（68d31b5）**：ContentBg 迁移时从「view 节点自身 addComponent(Graphics)」改 UIShape 子节点+setSiblingIndex(0)。Cocos 节点自身组件一定在子节点前渲染（背景保证），子节点 sibling 顺序在 ScrollView Mask/Stencil 下不保证→底纹盖内容。回退 viewNode.addComponent(Graphics) 原始方案。
- **点击厕所进错页（9d22b2d）**：onHomeCellClick 中 farm/trap/well/toilet 走 openBuildingGrid()（通用列表）而非各自详情页，与其余设施直达不一致。改走 openBuildingDetail(id)，并将该方法 private→public。
- **厕所详情页排版太分散（829cae3 → 13f1396）**：先 columns:4→1 单列（文字不被截断），再仿床铺页紧凑风格——信息合并多行 info cell + 操作加 `[动作]` 前缀，加 noTruncate。影响农田/陷阱/酿酒/水井/卫生间全部详情页。
- **排便粪便收集不到（e35bc1f）**：doShit 用 outputBox:'shit' 写入 boxSaveData['shit']（独立粪坑 box），背包只读 bag 故永远看不到。改为 outputBox:'bag'；feedBiogas 与沼气池展示 cell 同步改从 boxSaveData['bag'] 读粪便。佐证：data_studio 沼气站 require{'shit':1} 默认按 bag，粪便不在背包则沼气站也找不到。
- **卫生间详情页看不到沼气池（922a326）**：非 bug，沼气池 toiletUpdate 二级升级项 marshGasTank，需 toiletUpdate_level>1（淋浴间Lv1→沼气池Lv2）才显示操作 cell。原 level<=1 完全隐藏，对比洗澡未升级有锁定提示，体验不一致。改为 level<=1 显示 [沼气池] 需升级卫生间解锁(禁用) 提示 cell，与洗澡对称。升级逻辑 ActionBuilding.upgrade 用 `${type}_level`+=1 累加，makeUpgradeInfo 按 BUILDING_UPDATE_DATA[type] 的 key 顺序取下一级，正确支持多级。

## 铁律补充（已存 MEMORY.md）
- UILabel.estimateHeight 必须按 \n 分段；ScrollView.view 是 UITransform 非 Node 须取 .node；Node 无 insertChild 须 addChild+setSiblingIndex。
