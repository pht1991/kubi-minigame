# 2026-07-23 工作记录

## 地图 NPC 交互对齐原版（de47b3e）
- 现状：地图地点里点 NPC 直接 `ActionEvent.trigger` 只弹 toast。
- 改法：`EventPage.openEventDetail` 由 private→public；`OutdoorPage` 的 `event` 分支改为 `this.ctx.eventPage.openEventDetail(evId)`，复用与主页「事件」一致的详情页（对话+需求/奖励+触发按钮，不满足置灰）。
- 原版核对：`kubi-original-src/src/main.js` 的 `QuestComponent` 先展示 `d_1` 对话、再用 `GiveComponent` 的「给」按钮交付（不满足则 disabled），绝不会只弹 toast。

## 当前版 vs 原版 功能完成度对比（分析任务，未改代码）
- 数据层：item/event/place/mst/studio/dungeon 六个数据文件行数与原版完全一致（item 409/event 68/place 57/mst 105/studio 938/dungeon 485），数据为 1:1 移植。
- 原版 `AdvanComponent`=应用外壳(top栏时间/季节+状态栏+场景路由) 对应当前 `MainScene`；`BranchComponent`=出门/地图枢纽 对应 `OutdoorPage`。
- 已覆盖：制造(make/alch/magic/science)、烹饪、农田、酿酒、陷阱、大箱子、水井、建造、地图探索、地牢、战斗、交易、事件/任务链、背包/装备、技能/天赋、转生、时间/季节、存档。
- 缺口（partial）：
  1. 厕所 toilet 仅移植了建筑+升级数据(`toiletUpdate`/`showerPlace`)，但 `ActionBuilding` 无 排便/洗澡/沼气池 动作实现，卫生恢复(san)链路缺失。
  2. 盗贼：数据(ROBBER_DATA/ROBBER_INIT)+第10天自动解锁 `robberQuestGet` 已移植，但主动「基地被盗/偷窃」循环(STOLE/STOLE_CHANCE)未实现，仅保留叙事任务链。
  3. 夜间危险倍率未显著接入战斗/探索（冬季寒冷+水井结冰已接）。
  4. 原版睡觉的「躺着/wait」闲置动作当前无对应（仅睡觉）。
- 原版代码本地路径：`D:\Projects\demos\front_end\kubi-original-src\src\`（含 main.js 6491 行 + data_*.js），后续对比功能以该目录为准，勿再线上拉取。
