# 2026-08-06 工作日志

- 用户截图反馈 HarvestModal：① 按钮下间距还是大 ② 收获后按钮自动上移，但按钮下间距没跟着缩小。**根因**：`46661be` 给 HarvestModal 加的 `resizePanel` 公式 `targetH = 90 + 40 + listH + 86 + 30 = listH + 246` 本身偏大，且 `minPanelH=420` 把小内容场景锁死在 420；列表收缩（如全部拾取后只剩 1 行提示）时 `targetH < 420`，面板高度不再变化，按钮随 listH 上移但底部留白固定 ~154px。**修复(提交 bf8dfb4)**：精确重算 content 内元素占位——content 顶部距面板顶 90，按钮底部到 content 顶 = `listH + 116`，因此 `panelH = listH + 256` 刚好不裁剪按钮；`minPanelH` 从 420 降到 300。当 listH≈64 时空列表面板高约 320px，按钮到面板底部留白约 50px（ModalPanel content 底部固定 50px），消除之前 ~150px 死区。**双端已上线**：master `11491a6..bf8dfb4`、gh-pages `75faa79..929a517`（8/6 18:10），微信 `build/wechatgame/game.js` + 网页 `build/web-mobile/index.html` 均生成。
