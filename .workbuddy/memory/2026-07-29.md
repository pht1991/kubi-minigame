# 2026-07-29

## 详情页点击漏洞审查与修复（用户反馈"进入详情页后条目点击经常功能缺失/控制台报错"）
- 用 code-explorer 子代理全面审查各详情页点击链路（背包/大箱子/建筑/地点/技能/事件/各制造页 + ActionItem/ActionTrade/ActionCraft/ActionSkill + DialogPanel/ModalPanel/GridComponent）。结论：无未定义方法/缺 import/空引用必现报错；主要是功能缺失类。
- 修复1（BagPage.onItemClick canUse）：漏判 `bagSizeBonus` 与技能升级道具（`upgrade` 字段，覆盖指南类 farmUpgrade/strUpgrade… 与精华类 magicPotion/strPotion…）→ 背包扩容/技能升级道具点开详情无"使用"按钮（上一轮 ActionItem 已接通逻辑，UI 未暴露）。已加 `|| type==='bagSizeBonus' || !!upgrade`。
- 修复2（BagPage.onItemClick store）：`boxType==='bigBox'` 时仍显示"存入大箱子"但回调写死 from='bag' → 改为仅 boxType==='bag' 显示（bigBox 走 BigBoxPage 独立"取出"路径）。
- 修复3（ActionItem.use）：升级分支原 `if(upgrade && isDrink)` 只处理精华类，指南类（无 isDrink）点使用返回"无法直接食用"。改为 `if(upgrade)` 去掉 isDrink 限制 → 指南/精华均永久加技能。
- 修复4（BagPage.onItemClick canEquip）：补 `|| type==='neck'`（纯 neck 装备无装备按钮）。
- 修复5（EventPage）：事件"交谈→交付并触发"路径触发后底层事件列表不刷新（DialogPanel onCancel 空函数）。抽出 buildQuestPage()，doEventTrigger 成功后立即 navigator.replace 刷新 + emit UI_REFRESH；onClose 也改 replace。
- 编译验证：deploy-wechat.bat 无头构建通过，无 TS error。

## 背包布局重构：4列网格 → 列表视图（用户反馈"背包内文字显示不全"）
- **根因**：4 列网格下格子宽仅 138px，文字区 126px，20px 字体一行只能放 ~6 个汉字 → 物品名+副标题（如"斧头\n[尸]掉落物]"）被 CLAMP 截断。
- **方案**：用户选择列表视图（推荐）。重写 BagPanel.ts：
  - 布局参数从 `COLS=4, CELL_H=108` 改为单列列表 `ROW_H=52`（紧凑行高）。
  - 每行结构：**[28×28 类型色块] 名称(16px) + 类型标签/耐久条(12px) + 右侧数量(×N)**。
  - 行宽 ~568px（CONTENT_W 600 - 2×16 margin），可容纳 25+ 汉字，彻底消除截断。
  - 类型色块映射 14 种物品类型到不同颜色（武器橙、食物绿、材料灰、任务紫等）。
  - 耐久度工具显示绿色/黄色/红色耐久条 + "cur/max" 文字；非耐久物品显示类型中文标签。
  - 移除 UIGrid 网格布局依赖，改为手动垂直堆叠（单列无需网格引擎）。
  - 面板自适应：minPanelH 从 400 降到 340（列表更紧凑），minScrollH 从 240 降到 160。
- 编译验证通过，无 TS error。

## 已知未触发风险（本次未改，避免大改引入新 bug）
- 非标准 equipType 装备：BagPage.buildEquipCells 写死 ['body','hand','foot','head','neck']，但 ActionItem.getEquipSlot 返回任意 equipType。若数据出现标准槽外 equipType，装备后 UI「消失」。当前 data_item.ts 全部 equipType 标准，未暴露。
- effect 含 mp/str/def/agi/luck 的物品：canUse 显示"使用"但 ActionItem.use 只处理 temp/hp/full/moist/ps/san。当前数据未出现单含这些的 effect 物品。

## 背包行高调整（用户反馈"条目太窄"截图）
- BagPanel ROW_H 52→64、ICON 28→32、ROW_GAP 6→8；名称 16→17px 上移 y=10，副标签/耐久 y=-9~-10→-12；耐久文字 10→11px；minPanelH 340→380。名称与副标签垂直间距 17→22px。已提交 964e977 + 自动推 + 构建微信包。

## 两端自动构建接上（用户：切回 master，把两端自动构建接上）
- 之前用户在 gh-pages 分支上，找不到 push-wechat.bat（它在 master）。先 git checkout master。
- **重写 deploy-web.bat**（原版有 3 处坑：COCOS 路径写死错 `C:\Program Files\Cocos\Creator\...`、有 EnableDelayedExpansion、用 errorlevel 判成败）：对齐 deploy-wechat.bat 范式——真实 COCOS 路径 `C:\ProgramData\cocos\editors\Creator\3.8.0\CocosCreator.exe`、清空 ELECTRON_RUN_AS_NODE/NODE_OPTIONS、按产物判定成功、纯 ASCII、tee 写 build-web.log。
- **WEB_OUT 路径坑**：原 `deploy-web.bat` 认为输出在 `build/web-mobile/web-mobile`，实测 Cocos 输出在 **`build/web-mobile/index.html`**（web 平台不嵌套子目录，与微信 `build/wechatgame/` 同规律）。已修正。
- **新增 push-web.bat**（push + web/gh-pages）、push-all.bat（push + wechat + web/gh-pages）。push-wechat.bat 保留（仅微信）。
- **变量隔离 bug**：deploy-wechat.bat 无 setlocal 且把自身 ROOT 去尾斜杠 → 污染调用方环境，导致 push-all 第二次 `call "%ROOT%deploy-web.bat"` 路径拼错（`xxxdeploy-web.bat` 找不到）。push 脚本改用独立 `BASE=%~dp0`（带尾斜杠）调用子脚本解决。
- 实跑验证：deploy-web 端到端构建+推 gh-pages 成功（gh-pages 远程 e8ee43e）；push-all 双端全链路通过（master 远程 4e04f0a = 本地）。
- MEMORY.md「推送工作流约定」更新：AI 推送应跑 push-all.bat（两端都构建）。

## 继续审查：各系统逻辑漏洞与修复（用户：继续审查）
- 子代理 very thorough 审查除详情页点击外的全部系统（制造/种植/陷阱/酿造/野外/地牢战斗/技能/建造/休息/菜单/大箱子/交易/时间/存档/事件/战斗）。
- 确认并修复 4 个真实 bug：
  1. **MainScene 事件监听器泄漏（中→高）**：6 个 EventBus 监听器仅 PLAYER_DEATH 在 onDestroy 尝试 off，但用 `bind` 新引用删不掉，其余 5 个（SAVE_COMPLETE/OPERATION_DONE/HARVEST_READY/SEASON_CHANGE/ROBBER_RAID）完全没 off → 场景重建后重复弹死亡界面/重复存档/访问已销毁面板。改为 6 个 handler 存成私有箭头字段（`_onSaveComplete/_onPlayerDeath/_onOperationDone/_onHarvestReady/_onSeasonChange/_onRobberRaid`），onLoad/start 同引用 on，onDestroy 统一 off。
  2. **金币商人无限刷钱（中）**：`give==='gold'` 分支无冷却，每次点白送 max(默认100) 金币。加每日冷却（ensureStock 记录 `claimDay`，同日重复领取返回「今日收益已领取」）。
  3. **事件链 `data.event` 未解锁（中）**：ActionEvent.trigger 只标当前事件 experienced，不解锁 `data.event` 指向的下一事件，任务链一开始全可见可乱序触发。EventPage.buildQuestPage 加前置门禁：某事件是某「未完成任务」的连锁下一事件（其前置 experienced 为假）则暂隐藏，前置完成才解锁。
  4. **云端时间函数嵌套（低）**：`_fmtCloudTime` 的 `p` 写成 `(n)=>(n)=>...` 双层箭头，云端时间显示为 `[function...]`。改为单层箭头。
- **未改（待确认/设计层）**：地图怪物 `amount` 永不递减（amount 语义未明，改动或破坏狩猎玩法平衡）；两套战斗解算口径不一致（ActionCombat vs ActionDungeon.battle，统一需大改）；ActionCombat.useItem 仅应用 hp（战斗多状态药浪费非 hp 部分，设计简化）。
- 类型校验：`node node_modules/typescript/bin/tsc --noEmit` 全量检查**未报错于本次改动文件**（MainScene/ActionTrade/EventPage）；既有错误均为 `'cc`/`wx` 声明缺失与 data.ts 历史问题，由 Cocos 构建环境声明覆盖。
