# 2026-07-22 工作日志

## 修复：休息恢复状态值与展示不符（commit 663204b，已推 gitee master）
- 根因：GameManager.playerStateChange 是**增量函数**（`this.playerState[k] += delta`），但 RestPage 把 `Math.min(oldPs+rp,100)`（目标绝对值）当差值传入，导致 `ps = oldPs + (oldPs+rp)` 被 MAX_STATE 夹到 100 → 不论床铺等级一律回满；UI 文案写「+15体力」、提示按真实差值算，三者全对不上。
- 修复：RestPage 改为传差值 `playerStateChange({ ps: rp, san: rs })`，由内部累加+封顶；恢复量实际 = min(rp,100-oldPs) 与 UI/提示一致。
- 二次问题：REST_TABLE 仅 0~3 行，sleepPlaceUpdate 有 bed_1~bed_4（最高 level=4），`min(level,3)=3` 使满级 bed_4 恢复量同 Lv3 大床；补第 5 行 `[70,50,1]` 覆盖满级。
- 全项目 ts.transpileModule 校验 59 文件 0 错误。
- 注：其余 playerStateChange 调用方（ActionExecutor 产出/负值、TimeSystem 寒冷、ActionCombat/ActionDungeon -lost）均正确传差值，仅 RestPage 一处误传绝对值。

## 升级效果描述文案 + 床铺等级键 Bug（commit 179effb，已推 gitee master）
- 用户确认：升级效果文案放确认弹窗内，不放按钮（按钮文字过多）。
- BasePage.makeUpgradeInfo 弹窗效果行加「升级效果: 」前缀 + noTruncate；各页面补 opts.effectText：
  - 大箱子: 容量 +4（按钮由「升级 +4容量」改回「升级」）
  - 厨房 cookerUpdate: 烹饪耗时 ×0.8（每级提速约20%，COOK_SPEED_MUL=0.8）
  - 床铺 sleepPlaceUpdate: 升级后每觉恢复 体力+X / 精神+Y（按 REST_TABLE 下一级算，动态）
  - 户外 wellUpdate: 每次取水 +2 清水（8+等级×2）；toiletUpdate: 解锁/改善淋浴
- 修复 RestPage 等级键 Bug：原 `getBuildingLevel('sleepPlace')` 读 `sleepPlace_level`（从未写入→恒0），导致床铺等级/恢复量升级后始终显示「地板」档；改为 `getBuildingLevel('sleepPlaceUpdate')` 与升级系统写入键一致。
- 全项目 ts.transpileModule 校验 59 文件 0 错误。
- 注意 getBuildingLevel(name) 返回 `buildingSaveData[name+'_level']`：升级组键是 'xxxUpdate'（如 sleepPlaceUpdate），建筑 id 是 'sleepPlace'，两者不同；调用务必传升级组键。

## 厨房烹饪交互精简（方案B变体，commit 65d5290，已推 gitee master）
- 用户选择：主体照方案B，并补充4点：① 按背包材料列出可做的菜 + 可做数量；② 点击一次制作一份放回背包；③ 制作后按背包变化刷新列表（支持二次加工菜如 面包→木面包）；④ 菜谱书保持只读不可点；取料改动怎么简单怎么来。
- ActionCook.cook 新增 `from: 'bag'|'cooker' = 'bag'` 参数，取料/扣料来源箱可指定（checkHaveResource/changeItem 用 from），不再硬编码炊具箱。
- CookPage 重写为背包直烹：buildCookHubPage 按 COOK_DATA 按菜名聚合变体，对每个菜算 maxQty=各变体中 min(floor(bag[k]/need[k])) 最大者；cell `mk_${name}` 显示「菜名 可做N」，点一次 cook(recipe,1,'bag') 后 navigator.replace 刷新。
- 删除 openCookAdd/openCookStep1/openCookStep2/showCookResult 及 `_cookIngredient1`（原约8屏分步流程）。COOK_DATA 全为2食材配方，故不再需要选食材1/2。
- 注意：boxSaveData['cooker']（炊具箱）现已不被烹饪使用，沦为闲置结构（保留兼容，未删除）。
- 全项目 ts.transpileModule 校验 59 文件 0 错误。

## 公共进度条 + 背包裁剪确认 + Toast防截断（commit 1483277，已推 gitee master）
- 新增 `ProgressOverlay`（耗时动作播放真实时长进度条，结束才推进时间+产出）+ `ResultModal`（继承 ModalPanel 的确认弹窗）；`ActionExecutor.execute` 作为唯一改造点，timeNeed>0 弹条、结束 emit OPERATION_DONE；GameManager 加 `clampToBag/bagFreeGrids`，ActionMap 采集/拾荒按背包格数判断全取/部分(部分弹确认)；Toast 改 SHRINK 不截断；MainScene 新增 `_progressLayer`+订阅 OPERATION_DONE 决定 Toast/ResultModal。61 文件校验 0 错误。
- ⚠️ execute 为异步(timeNeed>0)：调用后立即返回成功，结果经 OPERATION_DONE 反馈；页面勿在调用后同步依赖产出状态。

## 修复：进入地图(赶路)错误弹进度条（commit f021dd4，已推 gitee master）
- 根因：`OutdoorPage.travelToPlace` 原 `ActionExecutor.execute({}, {}, timeNeed)` 仅跳时间，进度条改造后端点会弹条；且因 execute 异步，详情页在动画结束前就打开（时序 bug，改造前因同步执行被掩盖）。
- 修复：改用 `TimeSystem.useTime(()=>{}, timeNeed)` 同步跳时间，进入地点属场景切换非「耗时换产出」，不弹进度条；时间推进完再 openPlaceDetail。
- 结论：仅跳时间不产出的动作不要走 execute。全项目 ts.transpileModule 校验 61 文件 0 错误。

## 采集/拾荒改为「收获」选择弹窗（玩家自行取舍，commit a16b87e，已推 gitee master）
- 用户推翻上一版「系统按背包格自动裁剪(全取/部分丢弃)」逻辑，要求：弹窗展示全部收获+数量，底部「全部拾取」按钮，玩家可逐一点击材料取舍；单点材料按背包种类格容量判断（已有种类堆叠/有空格→自动放入，满则 Toast 提示），「全部拾取」=系统按同逻辑自动逐个点击（满则跳过提示未拾取项），「完成」关闭并提示遗留项。
- 实现：新增 `HarvestModal`（继承 ModalPanel）；`ActionMap.gather/scavenge` 改 `execute(...,{skipOutput:true,silent:true})` 不自动入背包/不 emit OPERATION_DONE，onDone 递减资源后 emit `HARVEST_READY{title,loot}`；`ActionExecutor` 新增 `skipOutput`/`silent` 选项，`GameManager.bagFreeGrids` 供容量判断；`EventBus` 新增 `HARVEST_READY`；`MainScene` 托管并订阅。移除原 `clampToBag` 调用与 `buildGetMessage`/`formatGetNames`。62 文件校验 0 错误。

## 修复 Btn 误导入导致 harvest_ready 崩溃（commit 50635b5，已推 gitee master）
- 报错 `Cannot read properties of undefined (reading 'primary')` @ HarvestModal.ts:64 → `Btn.primary`。根因：HarvestModal/ResultModal 写 `import { ModalPanel, Btn } from './ModalPanel'`，但 ModalPanel 仅转发导出 `C`，**未转发 `Btn`**（Btn 定义在 theme.ts，`export const Btn`），运行时 Btn 为 undefined。
- 修复：两文件改 `import { ModalPanel } from './ModalPanel'` + `import { C, Btn } from './theme'`（ResultModal 同上）。
- ⚠️ 铁律补充：用 `Btn.*` 预设一律从 `'./theme'` 导入，不要从 `'./ModalPanel'` 导入（后者只转发表头色 `C`）。ts.transpileModule 校验 62 文件 0 错误。

## 收获弹窗增加「背包」按钮 + 修复点击材料消失（commit 6637101，已推 gitee master）
- 用户纠正：原描述有误，真实需求=① 点击材料被收集后行会消失（bug）② 按钮区左侧增加「背包」按钮，点击打开背包界面。
- 修复「行消失」：根因在 touch 事件处理中同步 `render()`→`clearContent()` 销毁 ScrollView 节点，Cocos 事件系统在销毁中节点上继续派发导致行闪烁/消失；改为 `scheduleOnce(()=>this.render(),0)` 延迟到下一帧重建（toggleTake/takeAll 同理）。
- 按钮区改为三列：背包(左, Btn.neutral) / 全部拾取(中, Btn.primary) / 完成(右, Btn.confirm)。
- 「背包」经 `HarvestModal.onOpenBag` 回调打开背包：MainScene 注入 `() => this._bagPage?.openBagPanel()`；BagPanel.show 自带 setSiblingIndex 置顶，关背包后收获弹窗仍在。注意 theme.Btn 无 cancel，用 neutral。62 文件校验 0 错误。

## 收获弹窗再纠正：拾取后消失是预期，移除放回逻辑（commit 2417a9f，已推 gitee master）
- 用户再次纠正：**「点击材料后行消失」本就是预期行为（不是 bug）**，之前我误当 bug 去「修复」是理解反了。
- 正确设计：点击材料 tryTake 成功 → `delete _loot[id]` 从收获列表移除（消失）；不再有「再次点击放回」逻辑（删掉 _taken 集合与 toggleTake）。玩家如需整理已得物品，自行点「背包」按钮进入背包界面维护。
- 顶部提示改「点击材料放入背包（背包 X/Y）」；列表空时显示「已全部拾取，可点背包整理物品」。
- 仍保留 `scheduleOnce(()=>render())` 延迟一帧重建（防 touch 中销毁 ScrollView 节点，非为阻止消失，而是避免闪烁）。62 文件校验 0 错误。

## 收获弹窗材料数量(×N)不显示（commit 758499a，已推 gitee master）
- 根因：ModalPanel.mkInline 锚点为 (0,0.5)，x 是标签盒**左边缘**；原 x=w/2-18 再叠加宽度→盒落在行右界(312)之外，被 ScrollView 遮罩裁掉，故 ×数量 与「点击拾取」几乎不显示。
- 修复：`x = (w/2 - 18) - 宽度`，让标签盒整体落在行内（右对齐仍生效）。
- 顺手在 MEMORY.md 渲染铁律补一条 mkInline 锚点说明，避免重复踩坑。62 文件校验 0 错误。

## 背包格子展示优化：加深背景色 + 增大格子高度修复文字遮挡（commit 196c27a，已推 gitee master）
- 用户反馈：斧头等有耐久装备格文字被截断/遮挡（"[尸装备1"显示不全），背景色太淡格子边界不明显。
- theme.ts: cellBg (253,248,240)→(245,238,225), cellStroke (212,196,176)→(200,180,155)，cellBgDisabled/cellStrokeDisabled 同步加深，增强与面板背景(255,248,240)的对比度。
- BagPanel.ts: CELL_H 96→108（+12px）；有耐久时名字区 h=50/y=22（原44/18），耐久条 barY=-46/barH=7（原-39/6），耐久文字 barY+13（原+12），数量标签 y=-42（原-34）。纵向空间重新分配消除名字与耐久信息重叠。62 文件校验 0 错误。

## 修复真机屏幕适配：上下空白 + 弹窗遮罩未铺满（commit 038f4b6，已推 gitee master）
- 根因：MainScene 用 `ResolutionPolicy.SHOW_ALL` 保持 750×1334 宽高比，真机比例不匹配时上下留白；ModalPanel/ProgressOverlay/BattlePanel 全屏遮罩写死 750×1334 无法覆盖实际屏幕。
- 修复：
  - MainScene: `SHOW_ALL` → `FIXED_WIDTH`（宽度固定750，高度自适应填满屏幕）
  - ModalPanel: 新增 `_vsW/_vsH` 字段，onLoad 从 `view.getVisibleSize()` 取值，遮罩 rect/UITransform 均用动态尺寸
  - ProgressOverlay/BattlePanel: 同理全屏尺寸动态化
- ⚠️ 铁律补充：所有全屏元素（根节点 UITransform、遮罩 Graphics rect）禁止写死 750×1334，必须用 `view.getVisibleSize()` 取实际可见尺寸。62 文件校验 0 错误。

## 修复底栏贴底 + 存档指示器位置动态化（commit 2c46223，已推 gitee master）
- 根因：底栏 setPosition 写死 `-1334/2`，FIXED_WIDTH 下实际屏幕高度更大导致底栏悬空、下方大片空白；存档指示器同样写死 `(260,-560)`。
- 修复：
  - 底栏：`-1334/2 + BAR_H/2 + BOTTOM_MARGIN` → `-view.getVisibleSize().height/2 + BAR_H/2 + BOTTOM_MARGIN`
  - 存档指示器：`(260, -560)` → `(vs.width/2-95, -vs.height/2+85)` 始终位于底栏上方
- 中间内容区与底栏间距由 GridComponent ScrollView view 高度决定（场景预置），不受此改动影响。62 文件校验 0 错误。

## 接入微信安全区域适配（commit 421c1fb，已推 gitee master）
- 用户反馈：底栏被手机底部 Home Indicator 遮挡，未考虑安全区域。
- 新增 `_fetchSafeArea()`：`wx.getSystemInfoSync()` 取 `safeArea`，计算底部偏移 = `(screenHeight - safeArea.bottom) × (750/screenWidth)`（转为设计分辨率坐标），非微信环境静默跳过。
- 底栏/存档指示器 setPosition 均加上 `_safeBottom`；新增 `public safeBottom` getter。62 文件校验 0 错误。

## 完善安全区域适配：顶部状态栏下推 + 底部最小边距（commit 2254511，已推 gitee master）
- 用户反馈：安卓真机底栏仍贴边（无 Home Indicator 时 _safeBottom=0），顶部状态栏与胶囊间距过大。
- `_fetchSafeArea()` 扩展获取 `statusBarHeight` → `_safeTop`（设计分辨率坐标）。
- 新增 `_applySafeAreaToScene()`：`start()` 中将场景 `statusBarNode` Y 坐标减去 `_safeTop`，整体下推避开刘海/胶囊。
- 底栏加 `MIN_BOTTOM_MARGIN=8`，取 `max(_safeBottom, 8)` 保证安卓无 Home Indicator 时也留视觉间距；存档指示器同步。新增 `public safeTop` getter。62 文件校验 0 错误。

## 加固安全区域检测 + 底栏/状态栏尺寸调优（commits 33b8456 / 5c11424，已推 gitee master）
- 用户反馈真机「完全没变化」→ 99% 是旧包缓存；加固 `_fetchSafeArea()` 改 `wx.getWindowInfo()` 优先 + `getSystemInfoSync` 兜底。
- MIN_BOTTOM_MARGIN 8→16（设计坐标），安卓无 Home Indicator 也留明显白边。
- 真机确认底栏生效后用户反馈：1) 底栏 BAR_H=70/BTN_H=56 显窄矮 → 改 BAR_H=92/BTN_H=72；2) 状态栏离胶囊仍远 → `_applySafeAreaToScene()` 新增 FIXED_WIDTH 高度补偿 `offsetY = _safeTop + (vs.height - 1334) × 0.35`。62 文件校验 0 错误。

## 状态栏上推方向修正 + 调参（commits 6a67720 / 7df0f13 / 1f8d7ff，已推 gitee master）
- 用户指正：**方向反了**。Cocos y 正方向朝上，StatusBar 原 y=580 在屏幕偏上（距顶 87px）。FIXED_WIDTH 下屏幕更高（顶部 y=+vs.height/2），状态栏停 580 反而离胶囊远；之前 `p.y - offsetY` 继续下推越错越远。
- 修正：`_applySafeAreaToScene()` 改为 `p.y + offsetY` 上推，`offsetY = vs.height/2 - 667`（恢复设计距顶 87px），场景 StatusBar 节点 `_lpos.y=580` 通过 `p.y` 计入。
- 用户要求间距再减半 → 额外上推 44（距顶约 43px，commit 7df0f13）；又要求 44→88（commit 1f8d7ff，调参验证）。注意：580-667=-87，+88 后 `新y ≈ vs.height/2 + 1`，状态栏中心压在屏幕顶边，上半会被裁切。建议回调 60（略裁上沿）或 44（完整在胶囊下）。
- 用户反馈 88 仍靠下 → 要求 88×2=176（commit 1067b4e）。`新y = vs.height/2 + 89`，状态栏中心越过顶边约 89px，大概率推过头只露底条。若仍靠下继续加（如264）；若裁切回调 60~120。
- 用户反馈 176 仍离胶囊差一截，质疑偏移参照系（commit 1193bd0）。分析发现 StatusBar 父级是 Canvas（__id__:2），position 为 Canvas 本地坐标；之前在 p.y=580 基础叠加 offsetY 的方式依赖设计常量且可能被引擎适配修改。改为**绝对定位**：`y = vs.height/2 - TARGET_FROM_TOP(55)`，直接按距顶像素定位，不再依赖任何场景预设值。TARGET_FROM_TOP 可微调控制与胶囊间距。

## ⚠️ 关键根因：statusBarNode 绑定为 null（commit e54267a，已推 gitee master）
- **真因**：MainScene 的 `@property(Node) statusBarNode` 在场景 Inspector 中**从未绑定**，MainScene.scene 序列化为 `"statusBarNode": null`。运行时该字段始终为 null → `_applySafeAreaToScene()` 与 `_addStatusBarDebugBg()` 均早期 `if(!this.statusBarNode) return` 直接返回。
- 这意味着：**状态栏定位逻辑此前所有 commit（5c11424→1193bd0）从未真正执行**，状态栏一直停在场景预设 y=580——这才是"离胶囊太远/差一截"的真实原因（不是调参问题）；红框也不创建（用户"没看到红框"）。
- **修复**：onLoad 中运行时按名字查找并赋值 `this.statusBarNode = find('StatusBar') || this.node.getChildByName('StatusBar')`；并禁用其 Widget 避免覆盖 setPosition 的 Y；import 补 `find`。
- 教训：依赖 `@property(Node)` 绑定时，务必确认场景里已拖入节点；更稳妥是运行时 `find` 兜底。
- 调试红框/蓝底代码仍保留（`[DEBUG_TEMP]`），等用户看红框位置后调 TARGET_FROM_TOP 再删。
- **调试红框渲染坑（commit bec09c6）**：红框最初挂 `statusBarNode` 子树下完全不显示（被状态栏自身背景/Mask 遮挡或裁剪），蓝底挂 Canvas 根节点则正常。改为红框也挂 `this.node`，用 `statusBarNode.position.y` 实测 Y 绝对定位，精确覆盖真实状态栏。经验：动态调试覆盖层一律挂 Canvas 根节点，别挂业务节点子树。

## 状态栏定位改为 safeTop 动态计算（commits 9fcef0d→742643f，已推 gitee master）
- 用户反馈：真机红框与实际状态栏错位（红框标记的是胶囊区域，状态栏文字在红框下方很远）；但如果把状态栏移到**红框下沿**位置就差不多。
- 根因链：① `TARGET_FROM_TOP = 55` 写死 → ② 改为 safeTop 动态计算 → ③ **真机 `wx.getWindowInfo().statusBarHeight=0`**（模拟器正常）→ `_safeTop=0` 公式退化为旧值 → ④ **双 API 均返回 0**（`getSystemInfoSync` 也为 0）→ ⑤ 兜底 24px 物理偏小（状态栏顶部距顶仅 54px，目标红框下沿约 110px）→ ⑥ 兜底改为 53px 物理（×1.908≈101设计坐标）→ 真机确认位置正确。
- 最终方案：`_fetchSafeArea()` 同时取 getWindowInfo + getSystemInfoSync，statusBarHeight 优先非零；两 API 都 0 时兜底 53px 物理像素。`_applySafeAreaToScene()` 绝对定位：中心 y = vs.height/2 - _safeTop - 8 - nodeH/2。
- ⚠️ 铁律补充：**微信真机 `wx.getWindowInfo()` 可能返回 `statusBarHeight: 0`、`safeArea.top: 0`**（模拟器正常），必须同时调用 `getSystemInfoSync` 回退；两者都异常时给合理物理像素兜底值。

## StatusBar 纯代码迁移（commit 1262b1a，已推 gitee master）
- 用户决定状态栏也迁纯代码（与底栏/弹窗统一来源，消除场景预置黑盒）。StatusBar.ts 去掉 `@property(Label)` 7 字段，改由 MainScene.createStatusBar 手动赋值 + onLoad 兜底 `getChildByName`；顺手修 onDestroy 中 bind 引用不一致导致 off 失效的 bug（固定 `_onRefresh`）。
- 删场景预置 StatusBar 子树：node 脚本解析 MainScene.scene JSON 数组递归删 name==='StatusBar' 节点+8子节点+组件并重排 __id__（节点 81→54）。
- MainScene 删 `@property(Node) statusBarNode` 及 find/Widget 逻辑；新增 createStatusBar（new Node 建 StatusBar + Graphics 背景 750×120 + 7 Label 横排间距120）+ _mkStatusLabel；_applySafeAreaToScene 改用 this._statusBar；start 先 createStatusBar 再定位；新增 AD_TOP_OFFSET 常量（默认0）预留 Banner 广告位下推。
- 广告结论：微信广告是 SDK 原生层覆盖 Cocos 之上，与创建方式无关；纯代码化利于统一预留广告位边界。transpileModule 校验 0 错误。

## 清理孤儿资产（commit a1d5d51，已推 gitee master）
- 排查结论：层级管理器里 GridContainer+子节点/Canvas/Camera 仍由 GridComponent/setupUICamera 使用，**保留**；GameManager 场景节点是孤儿（GameManager 是纯单例类，代码不按节点名引用），其上挂 UITransform + 悬空脚本组件（statusBarNode，uuid 6fc6f9B 项目内已丢失）。
- 删除：MainScene.scene 中 GameManager 节点及其 2 组件（节点 54→51，重排 __id__，0 悬空引用）；assets/prefabs 下 Dialog/GridPanel/StatusBar 三空目录+meta（均已迁纯代码，目录仅 .meta 占位）。保留 GridCell（cellPrefab 动态克隆使用）。
- 结论：空 prefab 对包体积极小（无 .prefab 文件不打进包），但留着是编辑器黑盒/技术债，删后层级与资源管理器更干净。
