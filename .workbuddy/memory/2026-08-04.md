# 2026-08-04 工作记录

## 进度总结入库
- 新增 `PROGRESS.md`（系统/功能/界面/性能四维评估，含完整度条形图与已知缺陷），提交 master `48774bb`。
- 规模：72 TS 文件 / ~20.4k 行；14 Page、13 Action、8 UI 组件，无占位无 TODO。

## 优化改动（提交 a10ec78，已推 master）
1. **统一战斗解算**（修复不一致缺陷）：
   - `ActionCombat` 的攻击力/减伤/前缀/耐久公式抽为静态方法 `calcPlayerAtk(gm)` / `calcDamageReduce(gm)` / `applyPrefix(prefix, mst)` / `decayWeapon(gm, log?)` / `decayArmor(gm, log?)`；`init` 复用 `applyPrefix`。
   - `ActionDungeon.battle` 重写：复用统一公式，接入前缀加成、装备减伤、武器/防具耐久消耗（之前 `_prefix` 参数完全未用、无减伤、无耐久）。`explore` 现在掷前缀传入。
   - `ActionEvent.trigger` / `ActionMap.hunt` 调用从 `battle(id, {})` 改为 `battle(id)`。
2. **地图怪数量递减**：`ActionMap.hunt` 胜利后 `entry.amount -= 1` 并 emit `place_change`，与资源枯竭设计自洽（之前怪物不会耗尽）。
3. **性能优化**：
   - `GridComponent` 新增格子节点**对象池**（renderCells 复用、clearCells 回收，减少每次全量 destroy+instantiate 的 GC 抖动）；复用前 `node.off` 清除旧触摸监听防叠加。
   - `UI_REFRESH` 节流：`onUIRefresh` 用 `scheduleOnce(0.05)` 合并短时多次刷新为一次渲染。
   - `GridCell.onDestroy` 清理长按 `setTimeout`（低危泄漏修复）。
   - 删 `ActionItem.ts` 过时"装备（占位）"注释。

## 验证
- `tsc --noEmit`（项目 tsconfig，strict=false）：我改的 4 个 action 文件零报错；GridComponent/GridCell 报错全是本地缺 `cc` 引擎类型导致的级联（`Cannot find module 'cc'`），属环境性——Cocos Creator 真机构建正常（此前多次构建成功）。`scheduleOnce`/`unscheduleAllCallbacks`/`removeFromParent` 均为 3.8 标准 API。
- 真机/真构建未跑（需 Cocos 编辑器或 deploy 脚本）；对象池跨网格/列表模式复用、节流 50ms 手感建议真机验证。

## 待办（未做，按需）
- 上架前 separateEngine 切换仍等用户「上架通知」。
- 背包/详情页等历史 bug 见 2026-07-29 / 2026-08-03 日志。

## 跑 push-all 双端构建验证（用户「跑一次」）
- 首版后台调用误用了 `cmd //c 'D:\...\push-all.bat < nul'`，Git Bash 把 `//c` 后的反斜杠 Windows 路径拆坏，报 `'ubi-minigame' 不是命令` 并秒退，没产生任何产物（构建目录仍停在 8/3 17:36）。
- 修正：新增 `run-push-all.bat`（= `call "%~dp0push-all.bat" < nul`），从 Git Bash 直接 `./run-push-all.bat > build/pushall.log 2>&1` 启动（task 09NceC）。日志已确认进 `[1/3] git push` 阶段，正在跑 Cocos 微信包 + web 构建。结果待构建完成后核验（libVersion=3.16.2、双端产物、gh-pages 提交）。
- 教训已补进 MEMORY.md「推送工作流」：禁用 `cmd //c 'D:\路径\foo.bat'`，用 `./run-*.bat` 直接拉起。

## push-all 验证结果（task 09NceC，已完成 ✅）
- 微信包 `build/wechatgame/game.js` 时间戳 **2026-08-04 10:24:35**；`libVersion` = **"3.16.2"** ✅
- web `build/web-mobile/index.html` 时间戳 **2026-08-04 10:25:30** ✅
- gh-pages 推送成功：`5838618..3a230cf gh-pages -> gh-pages`，本地 fetch 后 `origin/gh-pages` = `3a230cf`（2026-08-04 10:25:44）✅（之前卡在 7/28 旧提交 3a67dcd，本次已推进）
- master 远程保持 `a10ec78`（空推，无新提交）。CRLF 警告为临时部署仓换行归一化，无害。
- 结论：双端构建 + gh-pages 全链路跑通，含战斗/地图/对象池优化已上线微信包与网页版。

## 网格文字竖排回归修复（用户截图反馈）
- 现象：主页所有双/多字中文标签（制造台/炼金台/秘术台/科研台/…）被按字符强制竖排成「制」「造」各一行。
- 根因：`GridCell.refresh()` 非列表分支把 `nameLabel` UITransform 宽度硬编码成 **40px**，叠加 prefab 默认 `enableWrapText=true` 与对象池复用残留的 list 模式 `overflow=CLAMP`，3×22px≈66px 中文被按 40px 换行。
- 修复（`edbcfbf`）：非列表分支显式重置 `lineHeight=28`、`overflow=NONE`、`enableWrapText=false`，宽度 40→**140**（格子 160 宽留 10px 内边距，可横排 4–6 字）。此坑已补进 MEMORY.md「GridCell」段。
- 重新部署（任务 UvCb7T）：master=`bdb49c4`（含 `run-push-all.bat` 包装器 + 修复）、微信包 `game.js` 2026-08-04 10:40:26、`libVersion=3.16.2`、web `index.html` 10:41:19、gh-pages `3a230cf..58732fd`（origin/gh-pages=`58732fd`，10:41:30）✅。修复已上线微信包与网页版。

## GridCell 竖排问题第二轮修复（用户截图反馈：「初次打开正常，导航回来又竖排」）
- 现象：edbcfbf 修复后，主页前 2 格（制造台/炼金台）首次渲染正常，导航回来又变竖排。
- 根因：仅重置 Label 属性治标不治本——`nameLabel.string = displayName` 是在 `enableWrapText=false` **之前**赋值的，Cocos Label 内部 assembler 按 string 赋值时的属性（wrap=true + width=40）**缓存布局**；后续修改属性不会自动重排。首次打开碰巧因其他机制重建布局看似正常，但对象池复用后 assembler 不再重建，缓存的竖排布局就保留下来。
- 彻底方案（46c5605）：GridCell 改为**纯代码构建**，彻底断绝 prefab 残留：
  - `onLoad()` 用 `new Node + addComponent` 按需建出 NameLabel/CountLabel/Badge/CooldownMask/NewBadge，所有属性在代码里显式控制
  - `refresh()` 严格按「布局属性 → 尺寸 → string」顺序设置，string 必须最后赋值
  - GridComponent 移除 `@property(Prefab) cellPrefab`，新增 `createCellNode()` 工厂方法（`new Node + addComponent(GridCell)`）
  - 同步从 MainScene.scene 移除 cellPrefab UUID 引用
  - 删除整个 `assets/prefabs/GridCell/` 目录（含 .meta）
  - 文档 `docs/Cocos编辑器配置操作指南.md` 同步更新（GridCell 章节改为「纯代码构建，无需预制体」）
- 教训已补进 MEMORY.md「对象池复用与 UI 组件状态泄漏」段（① 全状态重置、② string 必须最后赋值、③ 能纯代码就别用 prefab）。

## 纯代码重构重新部署（任务 taZRnN，已完成 ✅）
- master 远程 = `e6a2e8b`（纯代码重构 `46c5605` + 教训记录 docs commit，已推送）。
- 微信包 `build/wechatgame/game.js` 时间戳 **2026-08-04 11:06:56**；`libVersion` = **"3.16.2"** ✅
- web `build/web-mobile/index.html` 时间戳 **2026-08-04 11:08:00** ✅
- gh-pages 推送成功：`58732fd..9d309e6 gh-pages -> gh-pages`，`origin/gh-pages` = `9d309e6`（2026-08-04 11:08:14）✅
- 构建日志无任何 GridCell / prefab 残留报错（exit code 0）；主页格子改为纯代码构建后，导航回来不再竖排。

## GridCell 布局抽象为 CellLayout（用户选 A：支持方格/横条/内部文字排版混排）
- 新增 `assets/scripts/ui/cellLayout.ts`：定义 `CellLayoutKind('tile'|'bar')` / `CellLayoutStyle`（fontSize/lineHeight/align/wrap/noTruncate/span/width/height 均可覆盖）/ `CellLayoutContext` / `ResolvedCellLayout`，及 `resolveCellLayout(data, ctx, opts)`。
  - 优先级：layout 显式覆盖 > type 推导(list→bar) > defaultKind（页脚用 'bar'）。
  - 保留 `data.type:'list'` 字段向后兼容，100+ 处旧调用零改动。
- `GridCellData.layout` 新增字段（预设名 'tile'|'bar' 或 `Partial<CellLayoutStyle>` 覆盖对象）。
- `GridComponent.renderCells` 改为**逐格解析 + 流式定位**：每格按自身 ResolvedCellLayout 宽高定位，支持同页方格+横条混排；横条满宽用独立 `barWidth`（默认 660，独立于 columns，避免纯列表页 columns=1 时横条被压成 160 的回归）。`contentInnerW = max(方格网格宽, barWidth)` 保证列表页横条不被裁切。
- `GridCell.setLayout(L)` 下发解析布局；`refresh()` 只读节点自身 UITransform 尺寸排版（单一像素来源），移除 `isList` 硬编码两分支。
- footer 也走同一套（统一横条）；删除死属性 `listRowHeight/listPaddingX/listLineHeight`，改 `barHeight`；`MainScene.scene` 同步。
- **铁律（布局扩展）**：① 新增格子形态只需在 cellLayout.ts 加预设/覆盖项，勿在 GridCell.refresh 写死分支；② 横条满宽必须走 `barWidth` 而非 `columns*tileW`，否则 columns=1 列表页会塌缩；③ GridCell 排版一律读节点尺寸，不要反推预设数字（双源会不一致）。
- 提交 `ea23e40`，双端构建验证（task MZXc80）已完成 ✅。
- 微信包 `build/wechatgame/game.js` 时间戳 **2026-08-04 14:34:10**；`libVersion` = **"3.16.2"** ✅
- web `build/web-mobile/index.html` 时间戳 **2026-08-04 14:35:03** ✅
- gh-pages 推送成功：`9d309e6..701f0cb gh-pages -> gh-pages`，`origin/gh-pages` = `701f0cb`（2026-08-04 14:35:15）✅
- 构建日志无 GridCell / 报错（exit code 0）。方格/横条混排布局抽象已上线微信包与网页版。

## 页面/弹窗排版应用改造（用户确认 5 项全做）
勘察结论：弹窗体系（ModalPanel/DialogPanel）**不碰** GridCell，故「用改造后 GridCell 做排版」只适用于 16 个 Page；弹窗长文本裁切需在自身体系内修。
落地改动（提交待 push，commit 见下）：
1. **页面横条多行自适应**（`cellLayout.estimateBarHeight`）：按 `\n` 分段、中文字=1/英文数字=0.5 估算行数，返回 `max(ctx.barH, 文本高+padY*2)`，解决 bar 固定 120 高裁切多行（建造列表/技能详情）。
2. **DialogPanel 长选项自适应**（`estimateOptionHeight`，availW=520-16、最小 70）：render 改为按每行高度累加，UIVStack 自动排布变高行，修复升级需求被 70 高裁切。
3. **分组标题样式化**（`cellLayout.headerPreset`：满宽、居中小字 18、弱色、高 56）：BrewPage/TrapPage 的 `slot_label`/`place_label` 行、MenuPage 帮助页 4 个【】章节行，统一加 `layout:{kind:'header'}`。
4. **背包数量**：GridCell bar 模式也显 `×N` 角标（之前列表模式藏了数量 → 回归），位置 `cellW/2-22,0`。
5. **方格加图标占位**：GridCell 新增 `_iconNode/_iconLabel`（60×60 圆角彩块 cellIconBg + 居中字 cellIconText，默认隐藏）；仅 `isTile && iconPos==='top'` 显示，名字框缩小下移；`theme.ts` 加 `cellIconBg/cellIconText`；BuildPage 各 home 设施加 `icon` 字段 + `layout:{iconPos:'top'}`（制/炼/秘/科/炊/农/酿/陷/箱/井/厕/床）。
- tsc 校验（补 `node_modules/cc` 类型壳指向引擎 `.declarations/cc.d.ts`）：我的 8 个改动文件零新增错误；DialogPanel TS2416 `show` 签名、BasePage `noTruncate` 缺 DialogOption 字段均为存量（不在本次 diff），Cocos 转译不校验类型，不影响构建。
- GridCell 修复两处真实 tsc 错误：① fallback L 对象补 `iconPos:'none'`；② 误移入 `setCooldownMask` 的 `NewBadge/applyState` 两行移回 `refresh()` 末尾。

## 地图「**新**」角标偏移修复（用户截图反馈）
- 现象：户外页「出门」列表（学院/静谧森林/溪流）横条右上角的「**新**」红角标没贴到横条右上角，而是卡在原 160×160 方格右上角附近。
- 根因：`makeNewBadge()` 在 `onLoad/buildNodes()` 里按**默认 cell 尺寸 160×160** 算了一次位置 `(w/2-8, h/2-8) = (72, 72)`，之后 `refresh()` 只切 `active`，没按当前 cell 尺寸重设 → bar 模式流式改成 660×120 时角标位置残留。`setCooldownMask()` 已按当前尺寸重绘，NewBadge 漏了同一处理。
- 修复：GridCell.refresh() 里按当前 `cellW/cellH` 重新 `setPosition(cellW/2-8, cellH/2-8, 0)` 再设 active。教训补进 MEMORY.md「对象池复用」铁律 ④（onLoad 算的位置必须在 refresh 重算）。

## P2 弹窗公共组件抽离（用户确认：全部 5 项 + 复用 CellLayout 算法）
代码改动（已落地，待 commit/push）：`cellLayout.ts` 的 `estimateBarHeight` 加 `export`；`widgets/index.ts` 补 4 组件导出；`UIButton.ts` 加 `bgGfx` getter（按钮助手迁它，保留 `BtnRef`）；新增 `ModalRow/ModalInfoRow/ModalTab/ModalScrollList`；`ModalPanel.ts` 加 `createScrollList` 工厂 + `mkButton/mkBtn/mkTab` 迁 `UIButton`；改造 `DialogPanel/BagPanel/TradePanel/HarvestModal/EventDetailPanel` 用新组件（HarvestModal 修「先 clearContent 销毁列表再 setRows」顺序 bug：`render` 内 clearContent 后重建列表）。分析报告 `docs/弹窗体系分析.md` + 契合关系图（结论：复用 `CellLayout.estimateBarHeight` 算法层，不复用 `GridCell` 实体——对象池常驻生命周期与弹窗「每次 render 重建」冲突）。tsc 真实错误清零（47 总错全为存量）。

## ⚠️ P2 双端构建炸雷：`from './'` 裸目录导入（2026-08-04 17:xx 发现）
- 现象：`run-push-all.bat` 双端均失败，日志 `UnsupportedDirectoryImportError` / `不支持目录导入`，`build/wechatgame` 与 `build/web-mobile` 目录均未生成，game.js/index.html 缺失。后台任务 4U8Kim 卡在两轮 900s 轮询兜底（~17:27 自结束，无害）。
- 根因：`ModalScrollList.ts` 原 `import { UIVStack, UINode } from './';` 是**裸目录导入**。tsc 绕过解析到 `index.ts` 漏检；Cocos rollup/mod-lo 不支持目录导入 → 真机必炸。`UIVStack` 实际定义在 `UILayout.ts`（无独立 `UIVStack.ts`），故 `from './UIVStack'` 也错。
- 修复：`from './UILayout'` + `from './UINode'`。复跑 tsc 回 **47**（基线），`ModalScrollList` 零错误。铁律已补进 MEMORY.md「验证铁律」（禁止裸目录导入）。
- 收尾：等 4U8Kim 自结束后，**先 git commit P2 改动**（含 4 个新 widget 的 `.meta`），再 `./run-push-all.bat` 干净重建，核验 libVersion=3.16.2 + 双端时间戳 + gh-pages 推送。

## ✅ P2 上线验证通过（2026-08-04 17:43）
- commit `4b091e8` 已 push 到 origin/master（master 与 origin/master 同步）。
- 干净重建（后台任务 MvOYyf）成功：微信 `game.js`（17:42）、网页 `index.html`（17:43）均生成；`fix-build-config` 报告 `libVersion 已修复为 "3.16.2"`，构建产物 `project.config.json` 实测 `libVersion="3.16.2"`。
- gh-pages 推送 `adfd506..e2c2ea4 gh-pages -> gh-pages` 成功（网页端：https://pht1991.github.io/kubi-minigame/ ）。
- 全程无 `UnsupportedDirectoryImportError` / ERROR / 900s 超时兜底。双端 + 报告 + 记忆均已交付。

## ⚠️ 上线后用户报"文字展示有问题"（2026-08-04 17:47）
- **截图2 ModalTab 截断**：Tab 按钮文字"金币购买"只剩"币购"、"以物易货"只剩"物易"——根因：UILabel 不传 width → contentSize = `chars*size*0.6`（紧估），`enableWrapText=false` + `overflow=CLAMP` + `horizontalAlign=CENTER` 三连裁掉两侧。修：ModalTab 给 UILabel 传 `width: w - 16`。
- **截图1 背包第 1 行 3 行布局**：`斧头/[已装备]/耐久 20/20` 过高，与邻居行不齐。根因：BagPage 把 `isEquipped` 塞到 `name` 的 `\n[已装备]`，ModalRow 折行后变 3 行。修：types 加 `isEquipped?: boolean`、BagPage 移除 name 拼接、BagPanel 用 `parts.join(' · ')` 拼成 `[已装备] · 耐久 cur/max` 单行 subText。
- 提交 `20b3ce8`（4 文件 14+/4-），后台重建任务 VC8jeH 跑中。铁律已写入 MEMORY.md「验证铁律」：①UILabel 固定容器内必须传 width；②name 禁塞 \n badge 后缀。
- ✅ VC8jeH 干净重建成功（18:07 产物）：微信 `game.js` + 网页 `index.html` 均生成，`libVersion="3.16.2"`；gh-pages 推送 `e2c2ea4..6419563 gh-pages -> gh-pages`；`origin/master` 已同步到 `20b3ce8`。无报错/无超时兜底。文字修复已双端上线。

## ⚠️ 用户第二轮报背包红框问题（2026-08-04 18:14）
- **数量溢出条目**：ModalRow meta `pos(w/2 - 24) + width: 64` 假设锚点偏左，但 UILabel 锚点是中心 `(0.5, 0.5)` → meta 右边 = `w/2 + 8`，**溢出 8px**（截图红框位置）。修：`pos(w/2 - 12 - 35) = w/2 - 47`，width 70，align right → 右边 = `w/2 - 12`（行内 12px padding）。
- **"met" 英文 subText 泄漏**：ITEM_DATA 里大量物品（褐晶/白金钥匙/龙骨/水晶/宝石/金子/暗金/木头/干草/铁块/硝石/羽毛/毛皮/...）type 字段写的是简写 `'met'`，但 `BagPanel.getTypeLabel` map 只收 `mat`/`material`，`'met'` 未命中 → 原样输出 "met"。修：map 加 `met: '材料'`（与 mat/material 同义）。
- 提交 `1b523cf`（2 文件 6+/5-），后台重建任务 VC8jeH（待启动）；铁律已写进 MEMORY.md「验证铁律」：①UILabel 锚点是中心，pos+width 组合须按 `pos ± width/2` 重算右边距；②消费 ITEM_DATA type 的 map 必须**与 ITEM_DATA 同源同步**，新增 type 时同步加 map 条目，否则英文会原样泄漏到 UI。
