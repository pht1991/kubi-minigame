# 超苦逼冒险者 · 阶段二功能验证清单

> 适用工程：`kubi-minigame`（Cocos Creator 3.8 微信小游戏）
> 验证方式：用 Cocos Creator 3.8 打开工程 → 运行（模拟器 / 微信预览）→ 在界面逐项核对
> 说明：本清单覆盖阶段二七大系统（烹饪/建筑/地图/贸易/技能/地牢/事件）及背包/制造，并记录本次已修复的关键 bug。

---

## 0. 启动与编译

- [ ] 用 Cocos Creator 3.8 打开工程目录（首次打开会重建 `temp/`，稍候）
- [ ] 控制台**无任何报错**；运行后主页显示 8 个入口（状态/背包/制造/烹饪/建筑/地图/贸易/技能/地牢/事件）+ 顶部状态栏
- [ ] 所有操作结果以网格页内**灰色 msg 条**显示（无 `console.log` 噪音；`[GridCell]`/`[GridComponent]`/`[MainScene]` 诊断日志已全部清除）

---

## 1. 通用机制（每个系统都应验证）

- [ ] 顶部状态栏（HP / 满腹 / 水分 / 精神 / 体温 / 理智）随操作**实时变化**
- [ ] 每次操作会**推进时间**（时间系统生效）
- [ ] 材料不足 / 前置不满足时，msg 条给出失败原因，且**不崩溃**

---

## 2. 分系统验证（建议按此顺序）

### 背包（openBagGrid）
- [ ] 进入背包 → 对某物品执行 `use` / `equip` / `drop`
- [ ] 反馈显示在列表**顶部**的灰色 msg 条，物品数量正确变化

### 制造（openCraftGrid）
- [ ] 选配方 `make` → 消耗材料 → 产出成品（材料不足时提示）

### 烹饪（openCookPanel）
- [ ] 选含食材的配方 `cook` → 耗时后产出成品
- [ ] 食材被正确消耗，耗时随厨具等级缩短

### 建筑（openBuildingGrid）
- [ ] 建造一项 → 完成后 `buildingSaveData[id].own = true`
- [ ] 对该建筑 `升级` → 对应 `xxx_level + 1`，跳级被正确拒绝
- [ ] ⭐ **重点回归**：此前升级因前置校验 bug 会**全部失败**，现已修复（见 §4-1）

### 地图（openMapGrid）
- [ ] 进地点 → `采集`：扣体力（ps）、资源库存 −1
- [ ] ⭐ **重点回归**：采集此前因 `PLACE_INIT.clone` 未定义会**全部失败**，现已修复（见 §4-2）
- [ ] `拾荒`：随机获得该地点物品
- [ ] `狩猎`：抽取该地点怪物并进入战斗

### 贸易（openTradeGrid）
- [ ] 选商人 → 花金币购买其 `give` 物品
- [ ] 价格与 `ActionTrade.getPrice()` 展示一致；金币不足有提示

### 技能（openSkillGrid）
- [ ] 教师类技能：学习成本随等级递增；`skill[id] + 1`
- [ ] 天赋类技能：按 `cost + costInc × lv` 扣金币学习
- [ ] `one: true` 的技能只能学一次

### 地牢（openDungeonGrid）
- [ ] `进入`：初始化 `dungeonSaveData`（stairCount/roomCount/deepest）
- [ ] `探索`：约 60% 触发战斗 / 40% 开宝箱；按层抽怪与摇奖励
- [ ] `下层`：`stairCount + 1`，层数加深
- [ ] 战斗：简化回合制结算（武器攻击×技能加成 vs 怪物伤害×防御减免）；无武器时可能战败，属正常

### 事件（openQuestGrid）
- [ ] 触发事件 → 发 `get` + `chanceGet` + 可能的技能/怪物/事件连锁
- [ ] 已触发事件标记为 `experienced`，重复触发受控

---

## 3. 重点回归（本次修复的 2 个致命 bug）

- [ ] **§4-1 建筑升级可用**：`ActionBuilding.upgrade` 原用 `buildingSaveData[data.science].own` 校验前置，但升级链从不写 `own`（只写 `xxx_level`）→ 改为顺序等级校验。验证：连续升级可推进、跳级被拒。
- [ ] **§4-2 采集可用**：`data.ts` 的 `PLACE_INIT` 初始化 `for` 循环用了未定义的 `clone()`，会导致模块加载抛错、`PLACE_INIT` 实际为空 → 改为 JSON 深拷贝。验证：地图采集能正确读取 `town.tree.amount` 并 −1。

---

## 4. 已知限制（验证时心里有数）

- ⚠️ **装备栏独立 UI 面板未做**（按用户指令推迟到阶段二后）；当前仅在背包网格内有「装备」动作入口（`ActionItem.equip`）。
- ⚠️ 地牢战斗为**简化回合制**，非原版完整战斗逻辑。
- ⚠️ 贸易在网格 UI 下简化为**金币购买**（原版为易货估值系统）。
- ⚠️ `MainScene.ts` 及 UI 层依赖 `cc` 引擎类型，本地无 Creator 无法全量 tsc；需在 Cocos Creator 编辑器内做最终编译/运行验证（核心逻辑层已用 tsc 单独编译 + 32 项冒烟测试通过）。

---

## 5. 验证结论记录

| 项 | 结果 | 备注 |
|----|------|------|
| 编译启动 | ☐ 通过 / ☐ 失败 | |
| 背包/制造 | ☐ 通过 / ☐ 失败 | |
| 烹饪 | ☐ 通过 / ☐ 失败 | |
| 建筑（含升级回归） | ☐ 通过 / ☐ 失败 | |
| 地图（含采集回归） | ☐ 通过 / ☐ 失败 | |
| 贸易 | ☐ 通过 / ☐ 失败 | |
| 技能 | ☐ 通过 / ☐ 失败 | |
| 地牢 | ☐ 通过 / ☐ 失败 | |
| 事件 | ☐ 通过 / ☐ 失败 | |
